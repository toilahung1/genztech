PORT=3000
NODE_ENV=production
JWT_SECRET=your_jwt_secret_here

# Facebook App (optional - for OAuth flow)
FB_APP_ID=your_fb_app_id
FB_APP_SECRET=your_fb_app_secret
FB_REDIRECT_URI=https://your-domain.railway.app/api/facebook/oauth/callback

# Default FB token (optional - fallback for proxy routes)
FB_DEFAULT_TOKEN=

# OpenAI (optional - for AI generate)
OPENAI_API_KEY=
